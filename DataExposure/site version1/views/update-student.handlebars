<h1>Update {{student.fname}} {{student.lname}}'s student information:</h1>
<form id="update-student" action="/test_site/student" method="post">
    First name: <input type="text" name="fname" value="{{student.fname}}"><BR><BR>
    Last name: <input type="text" name="lname" value="{{student.lname}}"><BR><BR>
    Grade: <input id ="grade" type="number" name="grade" size="30" min="1" max="12" value={{student.grade}}><BR><BR>
    Band: 
      <select name="my_band" id="band-selector"> 
        {{#each bands}}
      <option value="{{id}}">{{name}}</option>
        {{/each}}
      </select>
</form>
<br>
<button onclick="updateStudent({{student.id}})">Update</button>
<script defer>selectStudent({{person.my_band}});</script>
<BR>
<P>Return to the <A HREF="/test_site/students">Students List</A></P>
<BR> <BR>
